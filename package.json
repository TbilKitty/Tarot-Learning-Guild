{
  "name": "the-fifth-card",
  "private": true,
  "type": "module",
  "scripts": {"build":"node build.mjs", "dev":"npm run build && wrangler dev", "deploy":"npm run build && wrangler deploy", "test":"node --test tests/*.test.mjs"},
  "devDependencies": {"wrangler":"^4.0.0"}
}
